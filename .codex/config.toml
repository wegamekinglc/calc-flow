# Project-scoped Codex settings. Codex loads this layer only after the
# repository is trusted. Durable repository guidance lives in ../AGENTS.md.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
